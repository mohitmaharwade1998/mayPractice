import { CurruncyrupeePipe } from './curruncyrupee.pipe';

describe('CurruncyrupeePipe', () => {
  it('create an instance', () => {
    const pipe = new CurruncyrupeePipe();
    expect(pipe).toBeTruthy();
  });
});
